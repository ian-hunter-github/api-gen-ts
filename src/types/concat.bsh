#!/bin/bash

# Usage: find . -name "*.js" | ./concat-js-no-imports.sh > output.js

while IFS= read -r file; do
  echo ""
  echo "//////////////////////////////////////////"
  echo "//  $file"
  echo ""
  # Output file content without lines starting with 'import '
  grep -vE '^\s*import ' "$file"
done
